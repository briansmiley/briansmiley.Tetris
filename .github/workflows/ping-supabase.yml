name: Ping Supabase Weekly

on:
  schedule:
    - cron: '0 12 * * *'  # Every day at 12:00 UTC
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping App Database
        run: |
          curl -sSf https://tetris.binarysmile.com/getHighScores/ALL